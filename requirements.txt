# ===== 音声ダウンロード =====
yt-dlp>=2024.12.0

# ===== 音声認識（ASR）=====
# Groq Whisper API（ASR_MODE=groq・デフォルト）: 無料・7,200分/日・超高速
groq>=0.8.0
# OpenAI Whisper API（ASR_MODE=api）: 有料フォールバック
openai>=1.40.0
#
# ローカル実行（ASR_MODE=local）が必要な場合は requirements-local.txt を使うこと:
#   pip install -r requirements-local.txt
# ※ torch(2GB+) / faster-whisper / pyannote.audio が入るため非常に重い

# ===== LLM クライアント =====
anthropic>=0.37.0
google-genai>=1.0.0              # Gemini Flash（無料枠・デフォルト）

# ===== 音声処理ユーティリティ =====
# pydub は OOM（メモリ不足）の原因になるため削除。チャンク分割は ffmpeg コマンドで実施。
numpy>=1.26.0

# ===== 環境変数 =====
python-dotenv>=1.0.0

# ===== ユーティリティ =====
tqdm>=4.66.0
loguru>=0.7.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
click>=8.1.0
requests>=2.32.0

# ===== 開発・テスト =====
pytest>=8.3.0
pytest-asyncio>=0.24.0
black>=24.0.0
ruff>=0.6.0
